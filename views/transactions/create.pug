h1 Borrow a book from the library

form(action ="/transactions/create" method="POST")
	label UserName:
	span &nbsp;
	select(name="userId")
		each user in users
			option(value=user.id)=user.name
	label BookTitle: 
	span &nbsp;
	select(name="bookId")
		each book in books
			option(value=book.id)=book.title
	button Borrow

